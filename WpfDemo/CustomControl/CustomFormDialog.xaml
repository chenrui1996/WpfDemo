<UserControl
    x:Class="WpfDemo.CustomControl.CustomFormDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfDemo.CustomControl"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <GroupBox
            x:Name="customGroupBox"
            materialDesign:ColorZoneAssist.Background="{DynamicResource MaterialDesign.Brush.Primary.Dark}"
            materialDesign:ColorZoneAssist.Foreground="{DynamicResource MaterialDesign.Brush.Primary.Dark.Foreground}"
            materialDesign:ColorZoneAssist.Mode="Custom"
            FontSize="18"
            Header=""
            Style="{StaticResource MaterialDesignGroupBox}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="80" />
                </Grid.RowDefinitions>
                <Border
                    Grid.Row="0"
                    Margin="0,10,0,10"
                    Padding="10">
                    <StackPanel x:Name="FormPanel" Margin="10" />
                </Border>

                <StackPanel
                    Grid.Row="1"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <Button
                        Width="100"
                        Margin="10"
                        HorizontalAlignment="Center"
                        Command="{Binding ConfirmCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding FormModel, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Content="确定"
                        IsDefault="True"
                        Style="{StaticResource MaterialDesignOutlinedDarkButton}" />
                    <Button
                        Width="100"
                        Margin="0,10"
                        HorizontalAlignment="Center"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        Content="取消"
                        IsDefault="True"
                        Style="{StaticResource MaterialDesignOutlinedDarkButton}" />
                </StackPanel>

            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
